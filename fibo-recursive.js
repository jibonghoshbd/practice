// fiboniccise serise
/* let fibo = [0, 1];
for (let i = 2; i <= 6; i++) {
    fibo[i] = fibo[i - 1] + fibo[i - 2];
};
console.log(fibo) */

// fiboniccese nth item on recursive way 

/* const fiboniccese = function (n) {
    if (n == 0) {
        return 0;
    } if (n == 1) {
        return 1;
    }
    return fiboniccese(n - 1) + fiboniccese(n - 2);
}
const fiboNumber = 7;
const nThItem = fiboniccese(fiboNumber);
console.log(nThItem); */

